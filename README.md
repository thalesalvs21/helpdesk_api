## Como executar
```bash
pip install -r requirements.txt
python app.py
```

O banco helpdesk.db é criado sozinho na primeira execução, dentro da pasta instance/ (comportamento padrão do Flask-SQLAlchemy). A API sobe em http://localhost:5000.
